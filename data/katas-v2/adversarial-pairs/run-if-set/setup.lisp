;;; setup.lisp — run-if-set (POSITIVE-condition edition).
;;; ADVERSARIAL PAIR NOTE: this is the WHEN twin. Its sibling kata
;;; `run-if-unset` runs the body when the test is NIL (uses UNLESS).
;;; Both share an instruction template differing in only one word.

(in-package :cl-user)
