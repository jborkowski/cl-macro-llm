;;; setup.lisp — bind-vars-star (SEQUENTIAL binding edition).
;;; ADVERSARIAL PAIR NOTE: this is the LET* twin. Its sibling kata
;;; `bind-vars` uses LET (parallel). The instruction differs by ONE
;;; word — "sequential" vs "parallel". A model reading the instruction
;;; superficially will pick the wrong expansion.

(in-package :cl-user)
