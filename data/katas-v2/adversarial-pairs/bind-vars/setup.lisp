;;; setup.lisp — bind-vars (PARALLEL binding edition).
;;; ADVERSARIAL PAIR NOTE: this is the LET twin. Its sibling kata
;;; `bind-vars-star` uses LET* (sequential). The instruction must be
;;; read carefully — the only differentiator is "parallel" vs
;;; "sequential" — and the expansion picks the right one.

(in-package :cl-user)
