;;; tests.lisp — generated from j14i/cl-ds row 205
;;; instruction: Write a Common Lisp macro `do-times-safe` that iterates a user-named variable from 0 below the limit, evaluating the lim
;;; category: capture-management | technique: once-only | complexity: intermediate
;;; quality_score: 1.0

(((do-times-safe (i (compute-bound)) (use i)) . (LET ((#:G1 (COMPUTE-BOUND))) (DOTIMES (I #:G1) (USE I)))))
