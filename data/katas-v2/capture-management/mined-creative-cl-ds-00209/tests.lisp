;;; tests.lisp — generated from j14i/cl-ds row 209
;;; instruction: Write a Common Lisp macro `tap` that evaluates its argument once, prints the value as a debugging side effect, and retur
;;; category: capture-management | technique: once-only | complexity: basic
;;; quality_score: 1.0

(((tap (compute)) . (LET ((#:G1 (COMPUTE))) (PRINT #:G1) #:G1)))
