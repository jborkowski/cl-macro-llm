;;; tests.lisp — generated from j14i/cl-ds row 147
;;; instruction: Write a Common Lisp macro `appendf-end` that appends a single element to the end of a list place, evaluating the new ele
;;; category: capture-management | technique: once-only | complexity: basic
;;; quality_score: 1.0

(((appendf-end lst (compute)) . (LET ((#:G1 (COMPUTE))) (SETF LST (APPEND LST (LIST #:G1))))))
